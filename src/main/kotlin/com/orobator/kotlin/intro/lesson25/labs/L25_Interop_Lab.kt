package com.orobator.kotlin.intro.lesson25.labs

// Create a class in Java called DogPojo. It should have a private field name
// that's a string. name should have a getter and a setter. Instantiate DogPogo
// from Kotlin. Note how you can call the getter and setter in the traditional
// Java way as well as Kotlin's property syntax.

// What is one the biggest difference between Java and Kotlin's exception
// handling? How does this difference affect interoperability?

// Create an example of a platform type throwing a runtime error when consumed
// from Kotlin.